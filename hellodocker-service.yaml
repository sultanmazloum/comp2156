apiVersion: v1
kind: Service
metadata:
  name: hellodocker-nodeport
spec:
  type: NodePort
  selector:
    app: hellodocker
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080